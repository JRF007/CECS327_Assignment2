I worked on implementing the two client files, rpc_client and subscriber_client. I fixed a problem where the clients were sending messages to the wrong ports. I also fixed an issue that involved the rpc messages wrapping incorrectly. One design change I made was changing the number of maximum worker threads defined in the config.json file. This helps increase the throughput of the messages sent between clients and server.
